<div class="content quote">
  <h5 class="quote_author">{{quote.author}}</h5>
  {% if quote.context %}
    <p class="quote_context">Contexte: {{quote.context | raw }}</p>
  {% endif %}
    <br/><p class="quote_content">{{ quote.content | raw }}</p>
</div>